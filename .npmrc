# Automatically install peer dependencies
auto-install-peers=true

# Save exact versions by default
save-exact=true

# Use package-lock for consistent installs
package-lock=true

# Show detailed logs during install
loglevel=notice
